# v0.18.9 Screenshot Seeded Data Plan

## Summary

Replace synthetic seeded validation data with applications, 2026 calendar entries, and current engagement statuses derived only from the supplied tracker screenshots.

## Key Changes

- Keep the 3 workflow organizations and 7 demo users unchanged.
- Seed 23 screenshot applications and 45 2026 VAPT engagements.
- Use `WHITEBOX` and `BLACK_GREY` assessment types according to the calendar image.
- Apply current engagement status from the pending-report tracker image only when a tracker row matches a calendar application and the engagement is in the January-June snapshot window.
- Do not seed scoping records, reports, findings, risk acceptances, ticket records, or synthetic `Seeded ...` applications.

## Test Plan

- Verify reset restores 23 applications and 45 engagements.
- Verify 22 `WHITEBOX` and 23 `BLACK_GREY` engagements.
- Verify owner/developer fields are populated from the tracker image where visible.
- Verify current status mappings place engagements into Kanban columns.
- Verify no synthetic seed applications or scoping/report/finding/risk records are restored.

## Assumptions

- Calendar image is the source of truth for application and engagement existence.
- Tracker image is the source of truth for current status, owner, and developer where matched.
- Month-only calendar entries use deterministic 5-business-day windows inside each month.
