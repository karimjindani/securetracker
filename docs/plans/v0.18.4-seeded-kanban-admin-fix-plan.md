# v0.18.4 Plan: Seeded Validation Baseline, Kanban Engagements, and Admin Page Fixes

## Summary

Implement deterministic seeded validation data, fix local schema/reset drift, clarify Organizations, and render Engagements as a Kanban board. Version allocation follows `AGENTS.md`: after fetching `origin/main`, `v0.18.3` was the highest allocated version, so this slice uses `v0.18.4`.

## Key Changes

- Seed exactly 3 organizations: NBP, Paysys Labs, and Apprise.
- Seed 7 demo users across those organizations.
- Seed 25 applications and 50 annual Whitebox engagements, two per application, spaced six months apart.
- Keep Apprise workload distributed at no more than five seeded assessments per month.
- Show annual calendar entries even after an engagement has started.
- Show Engagements as lifecycle Kanban columns with cards and Open actions.
- Add organization workflow role explanation and counts.
- Make `reset:seeded` apply Prisma schema sync before restoring seeded data.

## Test Plan

- `npm.cmd run db:generate`
- `npm.cmd run typecheck`
- `npm.cmd run lint`
- `npm.cmd test`
- `npm.cmd run build`
- `docker compose config --quiet`
- `npm.cmd run reset:seeded`
- `npm.cmd run test:regression`
