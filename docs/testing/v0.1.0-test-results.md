# v0.1.0 Test Results

## Scope

Initial scaffold verification for shared constants, backend health contract, frontend lifecycle contract, Prisma generation, typecheck, lint, test, and build.

## Required Commands

```text
npm run db:generate
npm run typecheck
npm run lint
npm test
npm run build
```

## Results

Passed on local Windows development environment.

| Command | Result |
|---|---|
| `npm run db:generate` | Passed |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed |
| `npm run build` | Passed |
| `docker compose config --quiet` | Passed |
| `GET http://localhost:3000/health` | Passed |

## Regression Checklist

- Shared lifecycle starts at `PLANNED`.
- `CLOSED` occurs before `GO_LIVE`.
- Only NBP Security Admin can close engagements in shared role helper.
- Paysys Security Admin can move closed engagements to Go-Live in shared role helper.
- Backend health endpoint contract reports `securetracker-api`.
- Frontend imports shared lifecycle constants.
