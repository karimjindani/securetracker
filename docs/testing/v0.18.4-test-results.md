# v0.18.4 Test Results

## Summary

This slice expands seeded validation data, fixes schema/reset drift, and changes Engagements to a Kanban board.

## Automated Results

| Check | Result |
|---|---|
| `npm.cmd run reset:seeded` | Passed; restored 3 organizations, 7 users, 25 applications, 50 engagements, 23 scoping records |
| `npm.cmd run db:generate` | Passed |
| `npm.cmd run typecheck` | Passed |
| `npm.cmd run lint` | Passed |
| `npm.cmd test` | Passed; shared, backend, and frontend unit suites passed |
| `npm.cmd run build` | Passed; Vite reported the existing large chunk warning for the PDF viewer bundle |
| `docker compose config --quiet` | Passed |
| `npm.cmd run test:regression` | Passed; 11 passed and 2 future-workflow specs skipped |
| `npm.cmd run test:data:cleanup` | Passed; removed only `REGRESSION_` data |
| Calendar duplicate-create check | Passed; repeated identical POST returned the same engagement id and left one database record |
| Frontend pagination smoke check | Passed; Calendar and Applications showed top pagination controls using Default Page Size |

## Covered Scenarios

- Seeded baseline has exactly 3 organizations.
- Seeded baseline has 25 applications and 50 Whitebox engagements.
- Every seeded application has two Whitebox engagements in the current year.
- The second seeded engagement for each application is six months after the first.
- Monthly Apprise seeded load does not exceed five assessments.
- Organizations and Users APIs load for System Admin; Users remains forbidden to Auditor.
- Calendar includes in-progress annual engagements, not only `PLANNED` records.
- Engagements include lifecycle states suitable for Kanban validation.
- List-heavy table pages use the Default Page Size setting and top-of-table pagination controls.
- VAPT Calendar supports Year, Starting Month, and Status filters.
- Calendar create is guarded against accidental duplicate submissions.

## Local Deployment Validation

- Backend health returned version `0.18.4`.
- Docker Compose services were running for frontend, backend API, PostgreSQL, Keycloak, MinIO, and Mailpit.
- Seeded reset completed after schema sync, leaving the local database in the v0.18.4 validation baseline.
