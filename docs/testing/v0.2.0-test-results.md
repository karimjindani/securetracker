# v0.2.0 Test Results

## Scope

Authentication, organizations, users, RBAC, frontend auth shell, Keycloak realm import, and SDLC regression gates.

## Automated Results

Passed on local Windows development environment.

| Command | Result |
|---|---|
| `npm run db:generate` | Passed |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed |
| `npm run build` | Passed |
| `docker compose config --quiet` | Passed |
| `npm run db:push` | Passed |
| `npm run db:seed` | Passed |
| Keycloak realm discovery at `http://localhost:18080/realms/securetracker` | Passed |
| Demo token exchange for `system.admin` | Passed |
| `GET /me` with demo token | Passed |
| `GET /organizations` with demo token | Passed |

## Regression Checklist

- JWT claims map to a SecureTracker user.
- Missing bearer tokens are rejected.
- Role guard allows required roles and rejects insufficient roles.
- Organization writes create audit records.
- Frontend navigation hides restricted admin routes for read-only roles.
