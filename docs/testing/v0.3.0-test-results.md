# v0.3.0 Test Results

## Scope

Version `v0.3.0` covers application inventory and annual/ad-hoc VAPT calendar planning.

## Automated Test Evidence

| Gate | Result |
|---|---|
| `npm run db:generate` | Passed |
| `npm run typecheck` | Passed |
| `npm run lint` | Passed |
| `npm test` | Passed |
| `npm run build` | Passed; Vite reported a large chunk warning for the frontend bundle |
| `docker compose config --quiet` | Passed |

## Test Coverage Added

- Application service creates records with `APPLICATION_CREATED` audit entries.
- Application service rejects invalid criticality values.
- Calendar service creates `PLANNED` engagement records with `CALENDAR_ENTRY_CREATED` audit entries.
- Calendar service rejects invalid planned date ranges.
- Shared tests verify application/calendar navigation and management permissions.

## Manual Regression Checklist

- Login through Keycloak.
- Confirm Applications and VAPT Calendar navigation appears for permitted roles.
- Create an application as Paysys Security Admin.
- Create a planned VAPT calendar entry as NBP Security Admin or Paysys Security Admin.
- Confirm read-only roles can view but do not see create forms.
- Confirm calendar entries remain in `PLANNED` status.
